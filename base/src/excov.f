      SUBROUTINE EXCOV
      IMPLICIT NONE
C----------
C  **EXCOV--BASE   DATE OF LAST REVISION:  07/23/08
C----------
C  EXTRA EXTERNAL REFERENCES FOR CVCNOP AND CVBROW CALLS.
C----------
      REAL ARRAY(7)
      INTEGER IDUM(*)
      INTEGER KEY,I,J,JJ,II
      LOGICAL LTHIN,LACTV,LNOTBK(7),LKECHO
      CHARACTER*8 KEYWRD,NOCOV
C
      DATA NOCOV/'*NO COVR'/
C
      ENTRY CVINIT
      RETURN
C
      ENTRY CVIN (KEYWRD,ARRAY,LNOTBK,LKECHO)
      CALL ERRGRO (.TRUE.,11)
      RETURN
C
      ENTRY CVGO (LACTV)
      LACTV=.FALSE.
      RETURN
C
      ENTRY CVCNOP(LTHIN)
      RETURN
C
      ENTRY CVBROW(LTHIN)
      RETURN
C
      ENTRY CVOUT
      RETURN
C
      ENTRY CVKEY(KEY,KEYWRD)
      KEYWRD=NOCOV
      RETURN
C
C     REFERENCES FOR THE PARALLEL PROCESSING SYSTEM.
C
      ENTRY CVGET (IDUM, I, J)
      RETURN
      ENTRY CVPUT (IDUM, I, J, II, JJ)
      RETURN
      ENTRY CVACTV (LACTV)
      LACTV=.FALSE.
      RETURN
      END
